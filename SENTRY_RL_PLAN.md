# 单哨兵语义地图在线 RL 方案

## 当前决策与项目边界

这个项目有两个不能混淆的学习对象：

1. **`tactical_rl`：主项目。** 红方主哨兵在可交互的 2D 规则环境中用在线 PPO 学习；它才承载“主动找人交战、压制、协同前压、按基地/前哨状态切换任务”等长期战术目标，并允许在仿真中超过历史数据。
2. **根目录 `rm_rl` / offlineRL：陪练行为模块。** 它从 2026 官方比赛日志中学习其他单位的移动、开火和选敌倾向，作为主哨兵训练时的队友/对手策略池。它不承担主哨兵的最终奖励，也不替主哨兵探索新打法。

因此，offlineRL 单独来看是**全局雷达状态条件下的行为策略**，不是完整世界模型；“可交互 2D 世界模型”应由以下两部分共同构成：

```text
语义地图 + 确定性比赛规则/状态转移  =  2D 环境
offlineRL / 脚本 / 固定行为策略       =  陪练实体的行为
在线 PPO 红方哨兵                     =  被训练的主策略
```

### 语义地图交付状态

RMUC 2026 的黑白障碍底图、彩色语义标注图和由此生成的
`semantic_map_aligned.json` **已经交付，代码已提供 `--map-json` 加载器并完成粗粒度环境冒烟**。
正式版本优先接受用户在 Foxglove/ROS `map` 坐标系中绘制并导出的地图；若多边形和占据栅格已经带有统一的 `frame_id`、原点和分辨率，则直接读取世界坐标，不再从 PNG 反推仿射校准。
像素级校验见 `sentry_tactical_rl/assets/semantic_map_validation.json`：当前 15 个区域
均能复现源标注，最低区域 IoU 约为 0.952。

但这仍不是“完整比赛仿真已经完成”：

- 黑白图与语义图的像素对应已经验证，加载器目前投影到 1 m 战术栅格；真实雷达/裁判坐标的平移、旋转和尺度仍需用基地、前哨站等已知地标校准；
- 语义多边形目前记录几何，不包含完整的占领序列、触发延迟、持续时间、冷却时间和规则效果；
- 基地、前哨站等语义区与实体障碍的重叠是预期现象，训练时必须分成硬占用层和可交互区域层；
- 隧道/道路/高地的精确裁判卡点、哨兵外廓通行掩码和增益点细节仍需补充。

因此当前可以开始“静态地图 + 规则状态转移”的世界模型开发，但正式 PPO 收敛训练要等坐标校准和第一版规则回归测试通过。

### 坐标校准流程

当前 `--map-json` 加载器只做了一个临时假设：图像覆盖同一个 `28 m x 15 m` 平面，按比例缩放到 1 m 战术栅格。`semantic_map_validation.json` 只能证明源标注图和 JSON 多边形互相一致，不能证明它们已经和真实雷达坐标对齐。

正式校准需要一张对应表，至少提供三个不共线的已知地标，建议四到六个：

```text
地标                  图像像素 (u, v)       雷达/裁判坐标 (x, y) m
红方基地中心          (用户标注像素)        (实测/协议坐标)
蓝方基地中心          (用户标注像素)        (实测/协议坐标)
红方前哨中心          (用户标注像素)        (实测/协议坐标)
蓝方前哨中心          (用户标注像素)        (实测/协议坐标)
```

校准步骤：

1. 在原始 `demo_map.png` 或黑白图上取地标几何中心，不要用叠加图上的文字位置；
2. 取得同一地标在雷达/裁判坐标系中的真实中心坐标；
3. 若场地俯视图只有旋转、平移、尺度差异，用最小二乘拟合仿射变换：

   ```text
   [x]   [a b tx] [u]
   [y] = [c d ty] [v]
                     [1]
   ```

4. 若图像存在透视或网格投影变形，用四个以上点拟合单应矩阵 `H`，并对每个点做齐次归一化；
5. 将所有语义多边形和障碍边界变换到雷达坐标，重新渲染叠加图；
6. 计算地标残差 `e_i = ||p_i^{pred} - p_i^{ref}||`，记录最大值、均值和每个地标误差；
7. 用三条已知路线验证：基地到前哨、前哨到中央高地、基地到对侧狗洞。路线不能穿越硬障碍，狗洞必须成为合法连通路径。

在 1 m 战术栅格上，建议地标残差先控制在 `0.2 m` 内；实车导航和窄通道验证应使用更高分辨率，不能只看 1 m 栅格的 IoU。没有真实地标坐标前，代码只能运行“按 28 x 15 m 比例缩放”的临时版本，不能声称已完成物理校准。

### 2026-07-27 规则状态机更新

`sentry_tactical_rl/match_rules.py` 已从环境中拆出并完成规则回归测试。当前已严格实现：基地 `5000 HP + 150` 虚拟护盾、己方前哨存活时基地无敌、前哨 `1500 HP`、每累计损失 `1000` 基地血量获得一次可积累重建机会、10 秒非工程重建占领、300 秒后的重建截止、前哨装甲 180 秒停转状态，以及官方终局比较顺序（基地、前哨历史/血量、攻击伤害、总剩余血量）。环境不再通过“区域人数差”直接磨前哨血，而由有效射击/脚本攻击调用同一规则函数。

语义加载器同时修正为以 JSON 声明的 `lower_left` 世界坐标保存占据层、锚点和多边形；之前的图像行号和世界 `y` 轴混用会导致它们上下镜像。现在保留每个区域的独立 mask 和归属，已接入己方补给区 10%/s 回血、中央高地 25%防御、前哨首次被毁后己方堡垒的 50%防御/冷却状态，并将起伏路保持为哨兵硬禁区。隧道的 50%防御 10 秒和 2 倍冷却 120 秒只能在“端—中—端”的裁判/地图序列验证后触发；当前 JSON 没有 sequence/group 元数据，代码不会把单次进入紫色多边形误当作触发。

`sentry_tactical_rl/sparring_adapter.py` 已建立“仿真快照 → 官方 offlineRL 161-D 裁判特征 → 冻结 IQL/BC tactical action”的唯一适配路径，并通过 `blue_sentry_iql_tactical/best.pt` 回归。它直接调用 `rm_rl.data.features.build_obs`，不复制固定槽位的字段顺序；`build_obs` 新增可选的单步物理时间输入，原始数据构建不传该参数，结果不变。当前环境只有红哨兵、两名红方步兵及蓝方英雄/步兵/哨兵的最小 roster，适配器会将缺失角色写为 `alive=0`，但**尚未**把这些输出接管脚本运动。完整多角色陪练前仍需补齐角色 roster、单位参数和 5 秒子目标的路径执行器。

这不是完整裁判系统：命中盒/弹道、经济兑换、复活、能量机关、完整姿态、堡垒储备弹药和真实坐标标定仍未完成。现有 PPO checkpoint 因观测和 target action 扩展而不兼容，继续只作为历史 smoke baseline，不可比较为策略优劣。

## 目标与边界

本方案只训练**哨兵的高层战术决策**，不试图训练所有兵种，也不让学习策略直接控制底盘。

目标是在比赛规则、哨兵真实可感知信息和语义地图给定的条件下，持续选择长期收益最高的：

- 目的地 `goal(x, y)` 或目标区域；
- 优先攻击的敌方目标；
- 交战/停火模式。

底盘控制、全局路径、局部避障、云台和射击闭环继续采用传统、可验证的算法。策略的作用是决定“**为什么去这里、为什么打这个目标**”，而不是学习“轮子怎么转”。

> 本方案优化的是“当前仿真模型、观测模型、规则与对手分布下”的策略；它不是对真实比赛全局最优的数学保证。仿真参数、感知误差与对手池必须持续校准。

## 总体架构

```text
雷达站可合法获得的全局比赛状态
             │
             ▼
雷达侧：结构化世界状态 + 语义地图 + 传统代价图/可达性/路径代价
             │                    ▲
             ▼                    │ 状态转移后的实体状态
红方主哨兵策略（唯一训练目标，在线 PPO）          陪练池：offlineRL / 脚本 / 历史策略
             │
             ▼
skill、目标区域/goal、target_id、fire_mode
             │
             ▼
传统全局路径规划 + 局部避障 + 安全约束 + 控制器
             │
             ▼
哨兵底盘 / 云台 / 发射机构
```

高层策略以约 1--2 Hz 更新即可；低层导航和控制以各自需要的高频率运行。高层指令带有效期，通信中断或目标不可达时，哨兵端进入安全回退（例如停火、撤至安全区或有限状态机保底）。LLM 不在该实时闭环中；它最多是后续的低频开发/策略配置工具。

### 雷达侧代价图职责

代价图属于雷达站决策组件，而不是让 RL 或哨兵凭经验猜测。雷达侧使用同一坐标系的静态场地地图、哨兵专属通行掩码、雷达敌方 track 与语义区域，生成：

- 硬禁区：障碍、边界、哨兵无法通过的狗洞/坡段；
- 静态软代价：贴障风险、预定义危险区/优先区；
- 动态软代价：敌方位置与置信度投影出的威胁场；
- 对候选 goal 的可达性、路径耗时/长度和累计路径风险。

RL 读取这些代价层和候选特征后选择 goal 与 target；雷达侧还应在发布前复核“目标可达”。哨兵端必须再用本地 ESDF、传感器和导航栈完成最终安全确认，雷达 action 不能绕过本地硬约束。

## 语义地图

语义地图不是给模型看的装饰，而是高层选择目的地的主要依据。仿真、训练和部署必须使用同一类地图语义。

### 静态层

- 场地边界、障碍、可通行区；
- 不同底盘专属的可通行掩码；
- 狗洞、起伏路、窄通道等地形；
- 前哨站、基地、增益点、补给点；
- 掩体、视线遮挡、射击优势区域。

对哨兵不能通过的起伏路、尺寸不允许的通道等，使用**硬禁区**，而不是一个很高但仍可能被穿越的软代价。敌方火力覆盖、开阔区域、拥堵等才是可权衡的**软代价**。

### 动态层

- 敌我实体的位置、速度、朝向、血量、热量、弹药、状态与置信度；
- 敌方火力/威胁热图、可见性和预计移动方向；
- 己方支援密度、队友任务和关键目标状态；
- 前哨站、资源、时间和比分等比赛状态；
- 从当前位置到每个区域的可达性、路径长度、预计耗时和路径风险。

本项目的主决策点在雷达站，按比赛允许的雷达全局视野组织双方位置、血量和建筑状态；不是把这些信息伪装成哨兵本体传感器。若未来改为哨兵端自治执行，则必须将对应实体替换为最后观测、置信度或概率分布，不能沿用雷达全局状态。

## 决策接口

第一版不模拟轮胎动力学，移动采用“沿可通行路径的距离 + 最大速度”的战术近似：
目标点可以是连续坐标，但执行器每个决策周期只沿规划路径前进有限距离；穿越中央高地或到达对方基地必须经过语义地图定义的狗洞/通道连接，不能用直线距离穿过障碍。速度可先固定为哨兵最大战术速度，后续再用实车标定。

建议把学习动作定义为下列混合的战术指令，而不直接输出电机控制量：

```json
{
  "skill": "hold_outpost | pressure | flank | support | retreat | resupply",
  "goal_xy_m": [x, y],
  "target_id": "enemy_id | none",
  "fire_mode": "hold | engage_if_safe | suppress",
  "valid_for_ms": 800
}
```

`goal_xy_m` 是连续战术目标，不是瞬时传送点。执行层必须做三项约束：

- **目标变化率限制**：连续决策间对目标点的位移不能超过配置上限；不能在两个基地之间 5 秒内反复切换；
- **最短保持时间/迟滞**：新目标只有在旧目标失效、不可达或收益明显超过迟滞阈值时才替换，避免来回振荡；
- **可达性投影**：目标先投影到哨兵可通行地图和路径图，再沿路径以最大速度推进。

策略仍可读取候选区域和路径特征来辅助连续坐标回归，而不是在整个平面上盲目输出：

- 前哨、补给、基地附近的关键锚点；
- 可攻击特定敌人的射击位；
- 可掩护队友或拦截敌方路线的位置；
- 可撤退的安全位置；
- 从可通行栅格/导航网格采样出的局部候选点。

当哨兵进入交战模式且目标有效时，执行层可生成目标周围约 3 m 的追击环：优先选择环上具有视线、低路径风险且可达的点；进入环内后保持相对距离或沿环移动，而不是无条件贴近目标。目标切换同样使用迟滞，避免追着当前正在交战的目标来回跳转。

对每个候选点预先计算路径代价、到达时间、暴露风险、可见目标、掩体质量、区域价值和撤退质量。高层策略应对 `(goal, target)` 对联合打分：

```text
score(goal, target) = 长期战术收益
                    - 路径总代价
                    - 暴露/死亡风险
                    - 热量与弹药机会成本
                    + 团队支援与目标物收益
```

传统规划器负责把获选区域中的点变成一条安全路径；若没有可行路径，则拒绝该目标、投影到可达点，并将 `goal_unreachable` 等反馈带回下一次策略观测。

### 语义 skill 与局势模式

RL 不会天然理解“前压”“回防”这些中文词；它看到的是地图通道、实体状态和奖励。但是，只要训练分布与奖励正确，它可以学习出等价的条件策略，例如：

```text
前哨濒危 + 敌方火力接近 + 己方支援不足 → 回防价值上升
己方优势 + 敌方暴露 + 路径风险可接受   → 前压/集火价值上升
低血或高热 + 存在安全撤退路径           → 撤退/停火价值上升
```

为获得可解释、可控的结果，建议保留人工命名的语义 skill（防守前哨、前压、侧翼、支援、撤退、补给、集火），而不是完全依赖无监督 skill discovery。RL 学习的是何种变量组合下选择哪个 skill、对应的 goal、目标车辆和持续时间；它不负责改变 skill 的安全边界。

端到端的 `(goal, target)` 打分仍然是主决策能力，skill 是对动作空间、候选点生成和解释的结构化约束，而不是取代 RL 的固定脚本。

第一版主哨兵需要被在线奖励和场景测试明确约束到以下偏好；这些是**主哨兵 PPO 的目标，不是 offlineRL 的模仿标签**：

- 默认保持主动交战倾向：有合法可达交战机会时优先找人、选敌、施压，不把“怕死/撤退”作为默认最优行为；
- 比赛时间达到 60 秒后，若敌方前哨仍存活，提高压制敌前哨区域和附近敌方单位的价值；
- “队友前压”只由队友位置坐标及其进入敌半场的比例定义，不需要 LLM 识别中文语义；满足条件时提高协同前压的价值；
- 己方基地血量降低时，提高己方半场拦截/交战的价值，但仍应寻找可打目标，不能退化为纯躲避；
- 第一版不把敌方基地作为主哨兵的直接攻击目标；双方基地血量先作为战术切换状态。是否开放基地攻击由后续规则和用户确认后再改；
- 增益点的坐标、占用效果和代价/奖励权重等待用户提供语义地图后接入，当前不硬编码。

## 仿真与在线 RL

训练环境采用战术级 2D 仿真。它不需要先复刻轮胎、完整弹道或所有裁判细节，但必须正确近似下列战术后果：

- 可达性、移动时间和地形限制；
- 视线、掩体、交战与暴露风险；
- 血量、伤害、热量、弹药与资源变化；
- 前哨站/基地/胜负等比赛目标；
- 敌我行为会随哨兵动作产生反应。

本项目的唯一在线训练目标是红方主哨兵 PPO，但正式陪练池必须先为所有会参与交互的非红方哨兵角色准备可验证的 offlineRL 行为策略。规则脚本只用于世界模型冒烟、数据冷启动和回归测试，不能作为正式 PPO 结果的唯一对手。红方主哨兵必须保留为 PPO 控制实体，不能被 offlineRL 控制器替换或删除。

每个回合随机化定位误差、通信延迟、速度、命中率、感知丢失和敌方风格，减少策略只利用某个理想化仿真漏洞的风险。

### 奖励

奖励应以长期比赛目标为主，而不是仅按“离敌人近”或“开火更多”设计：

- 正向：胜局、前哨站/关键目标收益、有效伤害、击杀、资源收益、关键支援；
- 负向：自身死亡、己方关键目标受损、无意义暴露、热量溢出、无效/不可达命令；
- 谨慎处理：路径代价和风险仅作为塑形项，不能压过赢得比赛的主目标。

这里的生存/风险项是防止无意义送死和不可执行命令的约束，不能把策略训练成消极避战。对“主动找人打”的要求应通过有效交战、压制区域、协同前压和比赛目标收益体现，而不是单纯奖励靠近敌人或无条件开火。

若需要运行时切换保守/激进风格，把风险权重、防守优先级等参数作为观测的一部分，并在训练中随机化它们；单纯在训练后改奖励，不能保证已训练策略会自动改变偏好。

### 当前版本的数学定义与训练曲线

下面先写**当前代码实际执行的低保真定义**，再写正式世界模型的目标定义。两者不能混为一谈。

#### 状态、动作和时间

当前每个决策步的状态可抽象为：

```text
s_t = (t, p_s, hp_s, heat_s, ammo_s,
       {p_i, hp_i, alive_i}_ally,
       {p_j, hp_j, alive_j, style_j}_enemy,
       hp/shield_red_base, hp/shield_blue_base,
       hp_red_outpost, hp_blue_outpost, outpost_destroyed_ever,
       match_phase_60/180/300, semantic_buff, map, threat)
```

其中 `t` 是离散决策步，当前正式配置为 `dt = 1 s`、`horizon = 420`。当前 demo 动作仍然是：

```text
a_t = (goal_index, target_index, fire_mode)
```

`goal_index` 选择一个地图锚点，`target_index` 选择敌方单位、敌方前哨或 `none`，`fire_mode` 为保持或交战。敌方基地的规则可攻击性已由状态机检查，但第一版 PPO target mask 暂不开放其直接选择。连续 `goal_xy` 尚未接入当前 checkpoint。

状态转移的基本形式是：

```text
s_{t+1} = T(s_t, a_t, u_t)
```

`u_t` 表示命中随机数、敌方风格和其他环境随机量。当前移动是低保真 A* 路径上的一格推进，不是完整底盘动力学。

#### 每秒陪练反应、长时域与状态充分性

“陪练每秒根据局势反应”不是要把未来 420 秒的所有对手反应预先塞进红方 state，而是环境在每一步的联合状态转移。正式形式应写成：

```text
a_opp,t ~ pi_opp(a | s_t, z_opp)
s_(t+1) = T(s_t, a_red,t, a_opp,t, xi_t)
J(pi_red) = E[Σ_(t=0)^419 gamma^t r_t]
```

其中 `z_opp` 是每局抽取的对手风格/策略快照，`xi_t` 是命中、感知等随机量。红方策略在时刻 `t` 只读取当前可获得的 `s_t`，选择当前动作；对手的下一步反应通过 `s_(t+1)` 反馈。它不需要也不可能枚举全部未来分支。这里所谓“world model”是**规则状态机 + 地图 + 陪练策略组成的可交互环境**，不是必须先训练一个神经网络去预测整局录像。

每个 state 本身没有“最优 reward”。单步 reward 是 `r(s_t, a_t, s_(t+1))`；PPO 的 critic 学习：

```text
V(s_t)    = 当前状态到比赛结束的期望总收益
Q(s_t,a_t) = 当前选择动作后到比赛结束的期望总收益
A(s_t,a_t) = Q(s_t,a_t) - V(s_t)
```

因此“60 秒后敌方前哨仍存活时前压”“己方基地濒危时回防但仍主动交战”等行为，可以由时间、建筑状态、敌我血量、路径风险和目标可达性共同决定；不需要为每一个秒数手工写一套脚本。

`420` 个 1 秒决策步对 PPO 并不异常。时间应以一个归一化标量 `t / 420` 和关键规则阶段标志（60/180/300 秒）输入，而不是展开成 420 维。实体使用固定角色槽位、`alive` mask、位置/速度/血量/弹药/热量等数值特征；语义地图和威胁层通过栅格编码器，候选 goal 用路径长度、到达时间、累计风险和射击价值特征编码。维度增加本身不是主要问题，训练覆盖不足和错误状态定义才是。

同一个**可见** state 下若对手仍会表现不同，说明存在未观测变量（对手风格、冷却/意图、过去行为、观测误差）。此时不存在唯一确定的“最优动作”，红方只能学习对该不确定性分布的最大期望收益或鲁棒收益。处理原则：

- 每局从冻结的 offlineRL、脚本、历史 PPO 快照池随机抽一套陪练，并在该局内固定；这使红方面对的是稳定的策略分布。不能让红蓝策略在同一批样本中同时快速更新，否则训练目标持续漂移；
- 将影响未来反应且雷达可得到的变量明确放入 state；对手内部变量若部署时不可见，训练时也不能提供给红方；
- 如果最近的交战/追击历史会影响对手行为，加入最近 `K` 秒摘要（速度、受击、目标切换、最近可见性），或使用 GRU/LSTM/Transformer 历史编码器；
- 用不同对手风格、命中率、延迟、感知丢失、初始资源和中局合法状态随机化训练；独立保留一组未参与训练的陪练快照做胜率评估。

长时域的实际难点是信用分配，而不是 state 维度。必须保留明确的终局胜负奖励，并配合建筑有效伤害、有效交战、无效动作和关键防守等可验证的中间事件；critic/GAE 和足够长的折扣有效时域负责把后期胜负回传到前期决策。训练可先用 60--120 秒局部交战/前哨场景做 curriculum，再训练完整 420 秒对局，并从随机但规则合法的中局状态启动一部分回合，避免策略只会开局。

#### 并行对局与陪练算力边界

并行训练指的是把同一个“地图 + 规则 + state/action 接口”的环境定义克隆为 `N` 个**独立的当前对局实例**。每个实例在任意时刻只推进自己的一局比赛；一局结束后该实例 reset，再开始下一局。它不是 `N` 个环境内各自同时塞入很多局，也不是让这些红方策略彼此通信：

```text
N 个环境当前 state             -> 红方 PPO 一次 batched policy forward
N × 非红方单位的当前 state      -> 冻结陪练一次 batched policy forward
每个环境规则/地图推进 1 s       -> N 份 (next_state, reward, done)
收集 rollout                    -> 用所有对局样本更新同一套红方 PPO 参数
```

例如 `N = 64`、每局 420 步时，一个完整批次有 `64 × 420 = 26,880` 个红方决策样本。若每局有 5 个非学习实体，则同批次最多产生 `134,400` 次陪练动作推理；这不是 134,400 次训练。陪练 offlineRL 在此处是**冻结前向推理**，不反传、不更新优化器；将同类角色的 161-D observation 堆成 tensor 后，可一次运行 `N × roles` 个样本，成本通常远低于真实物理仿真。

训练阶段不得为每个并行实例启动 Gazebo。在线 PPO 使用进程内的 2D 规则世界模型；Gazebo 只用于少量策略验证、ROS/控制接口联调和实机前安全测试。训练的主要潜在瓶颈通常是 Python 循环、每秒对每个单位重复 A*、视线判断和威胁图重算，而不是冻结 MLP 陪练本身。

实现与扩容顺序应为：

1. 先跑 8--16 个 2D 对局，验证规则、reward、陪练、动作 mask 和日志；
2. 实现批量陪练推理、静态地图/锚点路径预计算、威胁层增量更新后，扩至 32--64 个并行对局并实测 steps/s、CPU、显存与稳定性；
3. 只有在吞吐仍不足时才扩至 128 个以上，或进一步将环境核心向量化；不以“几千环境”为先验目标；
4. 红方高层决策可为 1 Hz；陪练可按 1--2 Hz 更新 skill/goal，在两次更新之间由路径执行器持续推进，避免为每个单位每秒重复完整规划；
5. curriculum 初期使用 60--120 秒局部场景，待策略和陪练稳定后再使用完整 420 秒对局。

#### 当前路径 cost

规划器对路径 `P = (x_0, ..., x_n)` 使用：

```text
C_path(P) = Σ_k [1 + C_static(x_k) + C_threat(x_k)]
C_risk(P) = mean_k C_threat(x_k)
```

当前威胁层对每个存活敌人 `j` 近似为：

```text
C_threat(x) += 0.55 * exp(-distance(x, p_j) / 4.0)
```

硬禁区不会进入路径；路径 cost 越大，代表路径长度、静态代价或敌方威胁越高。**当前 demo 的 `path_cost` 主要用于导航特征和曲线观测，并没有直接按比例从 reward 中扣除**；只有不可达目标会受到无效动作惩罚。正式版本需要明确是否加入：

```text
r_cost = -w_path * normalized(C_path) - w_risk * C_risk
```

不能一边把 cost 当作 reward，又在日志中重复扣一次。

#### 当前代码的单步 reward

当前环境实际使用的 reward 可以写成：

```text
r_t = -0.01                                  # 时间项
      + 0.035 * D_sentry                     # 哨兵造成的机器人伤害
      - 0.030 * D_taken                      # 哨兵受到的伤害
      - 0.012 * D_red_outpost                # 己方前哨受到的直接伤害
      - 0.020 * D_red_base                   # 己方基地受到的有效伤害
      + 0.030 * D_blue_outpost               # 敌方前哨受到的有效伤害
      + 0.025 * D_blue_base                  # 敌方基地受到的有效伤害
      + 0.003 * H_sentry                     # 己方补给区实际回血
      - 0.20 * I_invalid_navigation
      - 0.08 * I_invalid_fire
      - 0.02 * I_goal_switch
      - 8.0  * I_sentry_dead
      + 12.0 * I_win
      - 10.0 * I_loss
```

其中：

- `D_sentry` 是当前步红方哨兵对敌方机器人的实际伤害；
- `D_taken` 是当前步红方哨兵受到的伤害；
- `D_red_outpost`、`D_blue_outpost` 是经过建筑规则函数后的实际前哨伤害；
- `D_red_base`、`D_blue_base` 是经过“前哨存活时基地无敌”和护盾吸收后的有效攻击伤害；
- `H_sentry` 是补给区带来的实际生命恢复，不是单纯到达区域的奖励；
- `I_goal_switch` 当前只表示一次实际目标切换。全局 5 秒硬保持已经关闭，以允许追击时每秒更新 goal；连续目标的变化率限制尚未实现。

当前 demo 的攻击转移是：

```text
哨兵攻击敌方机器人或敌方前哨：
  可见/建筑可射线命中 ∧ 存活/未毁 ∧ 距离 <= 8 ∧ 弹药 > 0 ∧ heat + 15 <= 260 ∧ fire_mode = engage
  P_hit = 0.83 * exp(-0.07 * distance)
  命中时 damage = min(20, target_hp)

敌方机器人攻击红哨兵：
  距离 < 7 ∧ 有视线时，以 0.38 概率造成 min(14, sentry_hp) 伤害
```

当前已接入自动哨兵的 `300` 初始允许发弹量、`260` 热量上限、`30/s` 基础冷却、建筑护盾/前哨保护/重建/终局规则；仍没有真实命中盒、弹道、裁判增益、金币兑换和复活。因此这个 reward 仍只能作为训练管线基线，不能当作最终比赛 reward。

#### 正式世界模型的规则形式

正式版本应把建筑和单位状态显式写入：

```text
hp_k(t+1) = max(0, hp_k(t) - Σ_i damage(i -> k, t))
alive_k(t+1) = 1[hp_k(t+1) > 0]
t_{t+1} = t_t + 1 s
```

所有伤害必须经过同一个规则函数：

```text
damage(attacker, target, state) =
  0,                                  若死亡/不可见/超射程/冷却/目标非法
  weapon_damage * hit_probability,    否则
```

基地、前哨、补给区、隧道和堡垒的触发条件应单独定义，不能用“进入颜色区域”直接替代。第一版主哨兵可以在 `target_mask` 中禁止敌方基地作为直接攻击目标，但基地血量仍应进入状态，用于防守/压制策略切换。

#### 训练曲线的含义

`training_live.png` 有四个面板，横轴都是 PPO update：

1. **Reward**
   - `episode return`：完整一局 420 步 reward 的总和。rollout 尚未结束完整一局时会显示 `NaN`，不是程序错误；
   - `rollout reward`：最近 256 个采样步的平均单步 reward。

2. **Navigation cost**
   - `path cost`：最近 rollout 每步 A* 路径 cost 的平均值；
   - `path risk`：路径上威胁层的平均值。
   这两个量是代价图诊断，不等于 reward。

3. **Combat and objective terms**
   - `damage dealt`：哨兵造成的平均伤害；
   - `damage taken`：哨兵受到的平均伤害；
   - `blue outpost`：蓝方前哨受到的平均有效伤害；
   - `red outpost loss`：敌方造成的红方前哨直接掉血；
   - `blue base` / `red base loss`：双方基地的平均有效伤害；
   - `sentry healing`：己方补给区带来的哨兵实际回血。

4. **Constraint and exploration signals**
   - `invalid action`：不可达目标或非法开火动作比例；
   - `goal switch`：目标切换比例；
   - `switch blocked`：被目标保持约束拦截的比例；当前目标保持关闭后应接近 0；
   - `policy entropy`：策略分布的不确定性。熵快速降到 0，表示策略几乎只选一个动作，不等于策略已经学对。

真实 RMUC 地图 50 update 基线的曲线中，episode return 在约 `-11` 到 `+18` 间波动，末尾约 `14.8`；path cost 大致在 `10--12`，熵仍约 `3.5`。这说明真实地图已经接入且策略仍在探索，**不能据此宣称已经收敛或赢得比赛**。后续应看独立随机种子、胜率、前哨收益、伤害比和违规率，而不是只看一条 reward 曲线。

### 推荐训练方法

首个版本可使用 PPO：目标坐标采用二维连续分布（例如高斯经过地图投影），目标车辆和开火模式保留离散头。策略网络可由以下部分组成：

```text
多通道栅格语义地图 → CNN/UNet 编码器 ┐
敌我实体与历史状态    → Transformer/GNN ├→ goal-target 打分/策略头
候选点路径特征        → MLP            ┘
```

对硬禁区和不可达目标使用 action mask 或坐标投影。连续目标的变化率、最短保持时间和 3 m 交战环属于执行层硬约束，不能只依赖 reward 惩罚；策略网络仍可学习在这些约束下选择更优目标。

在已有可用 2D 规则仿真后，也可加入短时域 MCTS/滚动搜索：RL 先给出 Top-K 的 `(goal, target)`，再将它们向前推演数秒并重排。这使策略对代价图和规则的临时变化更敏感。

训练过程必须可观测。每个 PPO rollout 至少记录 episode return、逐步 reward、路径/威胁 cost、造成与受到的伤害、前哨血量变化、无效动作、目标切换次数和策略熵；训练入口用 `--live` 打开实时曲线，同时持续写入 `metrics.csv` 和 `training_live.png`。当前 demo 仍以离散锚点作为 PPO 动作，连续 `goal_xy`、3 m 追击环和 Top-K 重排属于下一阶段动作执行器升级，不能把当前离散 checkpoint 误称为已实现的连续控制。

## 官方数据与本仓库的作用

官方日志不再是主哨兵“必须模仿的老师”，因此不会构成主策略能力上限。主哨兵从仿真的在线 rollout 中学习如何在规则和陪练下获得长期回报。

当前数据源是 613 场、96 所学校的官方日志混合数据；状态包含全场实体和建筑。数据构建器已经按英雄、工程、步兵 3/4、空中和哨兵分别提取角色固定槽位的 `(state, tactical action)` 样本，而不是再把步兵策略迁移为所有兵种。每个角色的离线策略仍是全局雷达条件下的行为克隆/价值加权行为模块，不能把 held-out 动作指标误读成比赛强度。

### 2026-07-26 陪练策略训练状态

所有角色均使用同一份官方比赛划分，报告和 checkpoint 都写入 `rm_runs/<role>_iql_tactical/`：

| 角色 | 数据状态 | IQL 状态 | 结果文件 |
| --- | --- | --- | --- |
| 步兵 | 已有可用数据与 IQL 基线 | 已完成 | `rm_runs/infantry_iql_tactical/ROLE_REPORT.md` |
| 英雄 | 455,164 train / 50,412 validation 时刻，数据校验通过 | 已完成，早停于第 6,000 步 | `rm_runs/hero_iql_tactical/ROLE_REPORT.md` |
| 工程 | 455,164 train / 49,576 validation 时刻，纯移动且数据校验通过 | 已完成，早停于第 2,000 步 | `rm_runs/engineer_iql_tactical/ROLE_REPORT.md` |
| 空中 | 430,088 train / 47,068 validation 时刻，数据校验通过 | 已完成，早停于第 6,000 步 | `rm_runs/aerial_iql_tactical/ROLE_REPORT.md` |
| 蓝方哨兵 | 441,788 train / 48,740 validation 时刻，数据校验通过 | 已完成，早停于第 5,000 步 | `rm_runs/blue_sentry_iql_tactical/ROLE_REPORT.md` |

每个完成目录都保存 `best.pt`（按 held-out `action_mse` 选出的部署/回放 checkpoint）、`final.pt`、`train_log.csv`、`eval_log.csv`、`norm_stats.npz` 和 `ROLE_REPORT.md`。工程的开火标签在官方日志中恒为零，所以它是移动陪练，不能根据其 1.0 的 fire accuracy 声称其具有战斗能力。

`transition`、每项 held-out 指标的定义、如何选择 `best.pt` 以及为何不能把离线分数当成胜率，见仓库根目录的 `OFFLINE_RL_TRAINING_GUIDE.md`；同样的“指标如何看”章节已写入每个角色的 `ROLE_REPORT.md`。

本仓库的 BC、IQL、Decision Transformer 及数据可以用作：

- 训练虚拟步兵/哨兵行为策略，作为陪练；
- 从真实比赛提取常见路线、交战位置、开火频率和队伍风格；
- 标定仿真的移动、伤害、资源争夺与区域风险参数；
- 检查仿真中的打法是否明显偏离真实比赛；
- 构建历史策略池，供主哨兵与不同风格对手对战。

不能只用一个 BC 策略当陪练，否则主策略会学会针对其固定缺陷。正式版本应按英雄、工程、步兵、空中、蓝方哨兵等实际参与角色建立 offlineRL 数据集、checkpoint 和多个风格版本；守点/前压/抱团脚本只能在这些策略训练完成前做冷启动和回归基线。

当前 IQL/BC 的离线验证表明它们能复现导航/目标选择的一部分模式，但开火率仍低于真实记录。因此陪练池应把它们当作一种行为风格，而不是“已经学会主动强压”的唯一对手。主哨兵的强压偏好仍由在线 PPO 奖励学习。

当仿真中临时去掉某个兵种时，不需要为“少一个实例”重新训练该兵种策略：保持固定实体槽位，把该单位的 `alive=0`、HP/位置置为缺失，并对已移除目标施加 target action mask 即可。只有改变该兵种的规则、观测语义或希望得到新的行为风格时，才需要重新训练对应策略。

当前日志的观测包含较强的裁判全局信息。若把其中训练出的策略直接放入陪练池，应明确它可能是“全知陪练”；同时应训练或实现符合实际传感器遮罩的版本，避免主策略只适应不现实的对手行为。

## LLM 的位置

LLM 不是本方案的必要组件，也不建议负责高频的 `goal` 和 `target` 决策：官方日志没有足够的“状态—文字战术—正确动作”监督，且结构化数值地图、确定性约束和实时性并非 LLM 的优势。“队友前压”“基地低血量”“一分钟前哨未掉”等都应首先写成可验证的坐标/血量/时间特征和在线奖励，而不是让 LLM 猜测语义。连续坐标、可达性、遮挡、路径代价和热量约束仍应交给地图工具、RL/MCTS 和传统规划器。

若使用 LLM，第一版只把它作为独立的低频风格观察器，而不是 PPO 的观测输入或动作控制器。它可在每 10--30 秒或前哨失守、队友阵亡等关键事件发生时，读取由工具生成的结构化局势摘要，并输出固定 schema，供日志、可视化和离线分析使用：

```json
{
  "intent": "defend_outpost",
  "aggression": 0.25,
  "survival_priority": 0.9,
  "primary_objective": "outpost",
  "valid_for_sec": 15
}
```

第一轮训练不把这些字段注入 RL，避免 LLM 延迟、随机性和误判改变 PPO 的闭环。若后续单独做条件策略实验，再用消融对比验证其收益。除此以外，LLM 还可用于：

- 根据结构化比赛摘要建议保守/激进的策略参数；
- 生成或审阅行为树、技能定义和测试场景；
- 归纳日志中的对手风格。

LLM 的输出必须受固定 schema、工具与安全规则限制，不能绕过 action mask、可达性检查、导航或停火约束。若条件化 RL 已在各种地图、代价权重和陪练下表现稳定，LLM 未必带来胜率增益；应以独立仿真对局消融验证，而非默认将其放进比赛链路。

## 实施顺序

0. **校验并接入已交付地图。** 保留黑白障碍、语义区域和 JSON 几何的独立层，使用基地、前哨站等至少三个地标确认与 28 x 15 m 雷达/裁判坐标的变换；在校准前只做环境冒烟和状态转移测试。
1. 建立统一的地图表示：静态可通行掩码、关键区域、视线/掩体近似和哨兵专属硬禁区。语义区不得覆盖硬障碍层。
2. 把传统路径规划接入，能够输出候选 goal 的可达性、路径时间和风险特征；再接入用户定义的增益点和代价层。
3. 建立最小 2D 规则环境：沿路径距离移动、狗洞连接、目标变化率、3 m 交战环、攻击范围/扣血、建筑血量、时间和单位移除都必须有确定状态转移；先做规则/效用系统基线，以同一接口输出 `(goal_xy, target, fire_mode)`。
4. 为所有非红方哨兵参与角色建立 offlineRL 数据集：按角色固定槽位、观测、动作和 target mask，分别训练 BC/IQL/DT，并用 held-out 指标和回放可视化验收；当前仓库已有的步兵 checkpoint 只能作为其中一个角色。
5. 将各角色 checkpoint 接入统一陪练接口，构成多风格固定策略池；当前已完成 161-D 状态适配和蓝方哨兵 checkpoint 推理，下一步是补齐全角色 roster 并把 5 秒子目标接到路径执行器。规则脚本只保留为冷启动和回归基线。未完成角色执行适配前，不把红方 PPO 训练结果用于战术结论。
6. 用实时 telemetry 观察 reward、cost、伤害、目标切换和熵，保存 CSV/PNG，并建立训练异常告警（无效动作上升、cost 爆炸、熵过早塌缩）。此前的 `runs/sentry_tactical_rmuc2026/ppo_00050.pt` 仅标记为 smoke baseline。
7. 在陪练池稳定后，用哨兵专属障碍掩码、语义地图、路径代价和比赛状态训练唯一的红方主哨兵 PPO；再将当前离散锚点动作升级为连续 `goal_xy`，加入目标变化率、3 m 交战环和 Top-K + MCTS/CEM 短时域重排。
8. 用实车测试标定移动时间、定位/通信误差、可达性和射击效果；将参数反馈到仿真。

## 验收指标

### 陪练行为模块

- 在按比赛留出的官方数据上检查导航误差、开火 F1、具体敌方目标 Top-1；不只看开火准确率；
- 用关键局面可视化检查“前哨未掉、基地受压、队友前压”下的移动、目标和开火输出；
- 按兵种检查陪练的轨迹、开火率和交战分布没有明显偏离对应日志；脚本冷启动策略只能作为基线，不能当作最终陪练结果。

### 主哨兵在线策略

- 所有高层目标都通过哨兵可通行掩码与路径检查；
- 通信中断、目标失效或规划失败时能安全降级；
- 对不同陪练池、地图代价权重、噪声参数均有稳定表现；
- 相对规则基线和 BC 陪练基线，在独立仿真种子上的胜率、前哨收益、存活率和违规率提升；
- 在专门的场景回归中验证：60 秒后敌前哨仍存活时压制是否增强、队友坐标前压时是否协同、己方基地低血量时是否转向己方半场交战而非消极躲避；
- 训练曲线中 reward、伤害/前哨收益和约束指标可解释，且没有无效动作、路径 cost 或目标切换的持续爆炸；
- 实车上不存在依赖仿真全知状态、穿越禁区或无法执行的策略动作。
